services:
  frontend:
    container_name: kupi-podari-daj-frontend
    restart: unless-stopped
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    ports:
      - "127.0.0.1:8080:80"
    depends_on:
      - backend
    networks:
      - kupi-podari-daj-network
  
  backend:
    container_name: kupi-podari-daj-backend
    restart: unless-stopped
    build:
      context: ./backend
      dockerfile: Dockerfile.backend
    ports:
      - "127.0.0.1:3001:3001"
    depends_on:
      - postgres
    networks:
      - kupi-podari-daj-network
    env_file:
      - ./backend/.env

  postgres:
    container_name: kupi-podari-daj-postgres
    restart: unless-stopped
    image: postgres:17-alpine
    ports:
      - "127.0.0.1:5433:5432"
    env_file:
      - ./backend/.env
    networks:
      - kupi-podari-daj-network
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

networks:
  kupi-podari-daj-network:
    driver: bridge
      
    